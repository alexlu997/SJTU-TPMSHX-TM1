# TM1 主计算可靠性与性能

本任务的节点、固定验收标准与证据入口。Goal 于 2026-09-13 经用户授权建立；执行遵循
Stop That Shit 与 Ponytail。参考提交为 `1a8205211f31ba56d067169c51ade0c6753aa2bd`，
工作分支为 `codex/tm1/main-compute-20260913`。
最终实测结果和发布状态记录在 [PR #21](https://github.com/alexlu997/SJTU-TPMSHX-TM1/pull/21)
的说明与实际提交检查中；本文件中的 B0 和局部试验数值保留其原始测量身份。

## 范围与验收

- sCO₂ Diamond/Gyroid 双侧 2D/3D 完整计算，以及上海 2026-04-01 Gyroid
  水—空气实验的 2D/3D 完整计算。其余实验批次不自动加入硬性范围。
- 参数优化及快速定尺搜索延期，保留共享消费者的既有回归保护。
- 保持物理模型、水 Nu、数值标准、数据质量标记和历史失败；原生指标、
  实验对照和历史脚本指标分开记录。
- 测量前固定代表集与阶段集；先摸底估算总时长，再冻结重复策略。
  不通过删除困难成员或降低数值要求缩短测量。
- 正式基线 B0 包含必要正确性修复。性能目标在 B0 实测后、候选改动前冻结。
  原本合格而候选不合格是阻塞验收的新增回归；历史失败与未运行分别保留。
- 本机作为桌面性能测量机；macOS/Windows CI 证明相应兼容性，不代替真实桌面测量。
- 用户补充：数值求解效率为独立评估目标。分别分析 sCO₂ 与上海水—空气的
  二维/三维求解耗时、主要热点及重复工作；实施有明确收益的局部改进，或以
  测量依据说明没有合理的局部改进空间。GUI 响应改善不能代替该项结论。
- 用户批准执行上述节点及按授权审查、PR、CI、合并、同步本地；原始实验数据和
  私有数值配置不进入公开 Git。环境安装、物理范围扩大仍需另行明确授权。

## 节点

执行者为当前主控；用户已授权一个只读子代理独立审查。同机重型测量排他运行。

| 节点 | 状态 | 依赖 | 写入范围 / 完成标准 |
|---|---|---|---|
| G00 | 完成 | — | 本文、任务证据；版本/环境/数据/权限明确 |
| W10 | 完成 | G00 | 本地工况清单与必要工具；固定成员、配置、测量预算 |
| W20 | 实现完成，候选验收见 V00 | W10 | 实验入口及对应测试；参数生效、实际网格、指标身份明确 |
| R10 | 实现完成，候选验收见 V00 | G00 的代码/环境检查 | 计算控制器及直接测试；有界尾部日志、状态与输出恢复 |
| R20 | 实现完成，候选验收见 V00 | G00，真实工况验证需 W20 | CLI、现有 I/O 及直接测试；严格输出、导出故障可恢复 |
| C10 | 完成 | G00 | 现行测试辅助、历史说明、GitHub 主分支规则；必要 CI 生效 |
| B10 | B0 完成 | W20 | 薄测量工具、本地证据；分段耗时/内存/首次与预热明确 |
| S10 | B0 桌面序列完成，候选验收见 V00 | W20 | 现有主计算及桌面测试入口；切换、取消、失败后可重跑 |
| P00 | 已冻结，见下表 | B10@B0、S10 初测@B0、必要 R10/R20 修复进入 B0 | 本文及对照记录；分别冻结数值求解效率和交互目标及比较规则 |
| P10 | 四类局部筛选完成，候选验收见 V00 | P00 | 四类数值求解热点/重复工作独立评估；有收益的局部改进及交互改善分别验收 |
| V00 | 实测与发布记录见 PR #21 | W20/R10/R20/C10/B10/S10/P00 与选定 P10 完成 | 固定集无新增回归；审查、CI、真实桌面、合并与同步 |

## 实施记录与 B0 证据

证据执行目录为 `.cache/main-compute-20260913/`；交付归档位置为
`data/local_evidence/main-compute-20260913/`，入口为其中的 `REPORT.md`。
原始数据、逐工况配置和原生结果均保留在本地，不随公开 Git 发布。

- G00：起始树干净，`HEAD...origin/main` 为 `0/0`；已创建工作分支。
- 解释器：`/Users/luwenhuan/.venvs/sjtu-tpmshx-tm1-py313/bin/python`。
- `python -m sjtu_tpmshx.runs.tools.check_locked_environment requirements-lock.txt`：
  原生退出 0，71 个活动包匹配。
- `python -m pip check`：原生退出 0，无依赖冲突；沙箱 pip 缓存不可写警告不影响检查。
- 项目 Python/pytest/Numba 进程查询：原生退出 0，未发现其他项目计算进程。
- 主机：Mac16,6，arm64，36 GiB RAM、14 逻辑 CPU，macOS 26.6.2 (25G83)。
- 实际数据仓库为 `SJTU-TPMSHX-data`，干净 HEAD
  `06172513d1ca5effa55f7e204544a87d7cabf2b6`；声明 pin 为
  `1dcf916ed4468d8c365253f6e88d03b1546ecdfe`。两者实验目录差异为空，
  实际 HEAD 为包含该 pin 的布局合并提交，不把 pin 冒充实际 HEAD。
- 已成功读取上海标准批次 16 项及 sCO₂ D/G 表。
  当前旧脚本筛选为 D44/G42，历史 fixed-166 为 D43/G40 每维。
  差集 D48、G1/G37 单独保留，未据此改写历史固定集合。
- 用户已明确确认 sCO₂ 使用 fixed-166 原配置：交叉流局部端口、实验阻力、
  Nu 倍率 D=1.77/G=1.07。完整集合固定为 198 项（166 + 上海 32），
  `workloads.json` 保留原顺序、配置快照及参考记录。
- 日常 12 项为 sCO₂ D8/D19、G41/G7 各两个维度，以及上海 1/16 各两个维度。
  D8/G41 为既有代表 ID，D19/G7 为固定成员中实测热侧流量最大者；上海 1/16
  跨低/高空气流量。选择先于各工况计时。
- 测量预算固定：完整 198 项各执行一次状态覆盖；日常 12 项逐项预热一次、
  测量五次。冷启动选上海 1 二维和 sCO₂ D8 三维，三个隔离 JIT 缓存试验，
  分别记录空缓存、已有磁盘缓存的新进程和同进程预热后结果。
  桌面连续运行依次交替 D8 二维、上海 1 三维、G41 三维、上海 1 二维，
  五轮共 20 次；取消、导出故障与恢复额外记录，不混入正常重复统计。
- 上海 3D 首项摸底完成，但不能作 B0：代码正常返回且原生状态收敛，实际
  水流量约为表格 4.3317 倍。全开水入口面积为 182×42 mm，而现有速度换算
  借用 42×42 mm 的空气侧面积。用户随后确认沿用 GUI 的局部水口：
  y=42 mm、x=133–175 mm 进入，y=0、x=7–49 mm 流出，贯穿 42 mm 厚度。
  两维完整验证已共用该配置；原始失败证据保留在
  `pilot-shanghai-3d/`，不能因原退出 0 将其计为可信性能样本。
- 原清单保留为 `*.before-port-fix.json`；成员未变化。入口速度使用当前模型
  的单侧孔隙面积及入口密度。真实通量检查还复现了二维空气另用舍入气体常数、
  水侧变密度后入口流量漂移；已复用现有质量通量边界机制传递各流体入口密度，
  不改变物性关系或停止标准。对应接线、原生流量与焓输运 90 项通过。
- 二维入口密度修补会有意改变既有软件回归数值：空气示例 Q 从
  `31084.383039293898` 到 `31086.937427058772 W/m`（约 +0.0082%），
  混合流体两项同样保持原“不收敛”状态。新参考经原生入口流量核验后写入
  现行集成测试，原 `1e-10` 比较容差未放宽；旧值见 `1a820521`，完整
  新值和流量证据在 `inlet-correction-reference.json`。三维空气示例和
  `golden_3d.json` / 元数据均保留原数值。
- 真实 CLI 正常算例退出 0、耗尽迭代算例退出 2；整个 stdout 均可严格解析
  为 JSON，非收敛状态和警告保持明确。证据为 `cli-real-3d*` / `cli-exhausted-3d*`。
- R10/R20 定向 79 项通过；日志尾部丢失/超限、GUI 输出混入及非法 JSON
  均先保留失败复现。导出第二文件失败复现 2 项失败，修补后对应两组共 42 项通过。
  文件最终发布恢复、上海真实准备接线等 16 项通过。
- 主分支三项实际检查已设为 required 且回读核验；管理员受约束、禁止强推/删除。
  尚无具名多人分工；平台非作者批准人数为 0，不冒称存在独立人工审批。
- golden 配置提取与原函数（含 override）字典相等；历史数值未改。
- 快测原生退出 1：3061 通过、5 跳过、100 未选，唯一失败为 CLI 可选结果的
  mypy 类型收窄。该点修正后，类型/CLI/文件恢复共 19 项通过，原生退出 0。
- 修补后完整快测原生退出 0：3066 通过、5 跳过、100 未选，433.36 秒；
  日志为 `correctness-fast-v2.log`。真实公共集成及入口核验原生退出 0：
  41 通过、1 跳过，72.36 秒（`correctness-integration-v2.log`）。
- 正式 B0 为 `6e1c7ad34312d62ca4c205e644cbb042334b5447`，由独立工作树执行；
  各记录的代码来源为该提交且 tracked_changes=false。
- `B0-full/` 的 198 个固定成员全部完成并满足软件状态与入口流量检查，原生退出 0。
  每项 Case/Result/metrics 已保存、回读并核对身份；最大入口流量相对偏差
  `5.57664e-7`，低于既有 `1e-6` 要求。摘要为 `B0-full-summary.json`。
- 原生 Q 与实验的 RMS 相对误差单独保存在 `B0-experiment-errors.json`：
  上海二维/三维 2.08%/3.68%；sCO₂ Diamond 二维/三维 30.03%/35.92%，
  Gyroid 二维/三维 20.84%/41.40%。二维 Q 按实际 42 mm 厚度换算为 W。
  已核对实验阻力和固定 Nu 参数沿当前主链传递；上述软件资格不代表实验精度
  通过，本轮不修改模型或重标定。该差异由项目负责人在后续准确性阶段处理。
- `B0-desktop/`：真实 Cocoa 桌面正常 20 次、取消后恢复 3 次，原生退出 0。
  反复切换后的配置、实际网格、必要诊断和指标相等。准备、SIMPLE、LTNE、
  关闭窗口四项均只有一次取消终态；请求到空闲约 0.168/2.132/0.005/2.237 秒。
  两次三维导出故障保留旧产物及内存结果，重试成功。
  首轮编译后 RSS 约 3.53–3.58 GiB，未出现逐轮持续增长；该 20 次序列不证明
  无限长期无泄漏。上海二维预热后计算期间最大事件间隔 3.60–4.17 秒。
- 首个维度的桌面点击计时含观测器导入，不能用于产品延迟结论；此限制及修正后
  驱动见 `desktop-measurement-notes.txt`。该序列的初始化计时也含数值测试模块导入，
  实际应用启动改由无这些导入的 `startup_probe.py` 单独测量。GUI 接受的实际配置另存
  `gui-accepted-manifest.json`，按该快照检查公共调用等价，不把 GUI 外迭代默认值
  和完整实验清单的预算当作相同配置。
- 用户已允许一个只读子代理审查本次基线及后续性能差异，审查与本机测量分开进行。
- 基线独立审查提出两项，均已修补：保存/回读失败必须撤销性能样本资格，
  README 二维示例参考同步入口密度修补。故障检查在 B0 上 2 项失败、修补后
  2 项通过（`review-measurement-before.log` / `review-measurement-fix.log`）。
  B0 仅将 execution=completed 且资格为真的记录计入性能统计；此修补不改变
  成功计算与计时，失败证据保留，未替换已固定 B0。
- Draft PR #21 已建立；`b614418` 的 macOS 14 / Python 3.13、Windows 2022 /
  Python 3.12 和 minimal-postprocess 均通过（`ci-initial-complete.json`）。
  该结果只证明当前正确性快照，最终性能候选仍须在其实际提交上通过必要 CI。
- B0 重复测量 72 项（12 项各预热一次、测量五次）及 18 次隔离缓存试验均
  原生退出 0；摘要为 `B0-daily-summary.json` / `B0-cold-summary.json`。
  sCO₂ D8 三维空缓存进程约 103.6–108.6 秒；已有磁盘缓存首项约 23.8–24.2 秒，
  同进程后续约 23.3–24.3 秒。首次模块/JIT 导入包含在完整时间内，不能只取
  `solve_inclusive` 代替首次总等待。
- 无数值测试模块导入的真实 Cocoa 启动三次，首绘约 1.42–1.50 秒，可交互
  约 1.52–1.60 秒（`B0-startup-*`，原生退出均为 0）；四个 GUI 接受配置
  通过公共主链回放，五项显示指标及收敛状态在既有容差内相等
  （`B0-gui-public-comparison.json`）。
- 数值定向剖析保存在 `B0-profiles-v2/`。首次观测器在 Python 3.13 中错误地
  启用了嵌套 cProfile，失败记录保留在 `B0-profiles/`，不计入软件或性能合格数。
  修正为单一剖析器后四类均完整求解。并行调用及资源采样可使剖析累计时间
  重叠；热点判断结合独立边界时钟，不相加为墙钟，不用剖析时间计算提速。

## P00：性能目标与比较规则（候选改动前冻结）

下列秒数是 `B0-daily/` 各工况五次预热后求解时间的中位数范围，包含原生捕获，
不含前处理、应用映射和文件写入。不同维度的网格不同，横向比值不是加速比。

| 独立评估对象 | B0 求解中位数 | 主要工作与待验证的局部候选 |
|---|---|---|
| sCO₂ 二维，4 个代表 | 3.69–6.36 s | 焓步骤约 90% 以上；核对 EOS 状态查询及已有温度复用，保留状态拒绝、耦合能量检查 |
| sCO₂ 三维，4 个代表 | 17.64–37.84 s | SIMPLE/AMG 与焓步骤；检查初始双线程调度的实际收益及重复工作，不缓存变化的数值矩阵或 LU |
| 上海二维，2 个代表 | 17.48–23.24 s | LTNE 约 13.51–17.50 s，包含必要的加密网格诊断；尝试简化固定四面分支/导热计算，保留 Picard/SOU 更新次序 |
| 上海三维，2 个代表 | 0.382–0.394 s | 小网格 SIMPLE 稀疏直接解占主导；检查现有排序等局部设置，避免为极小绝对收益引入复杂机制 |
| 上海二维交互 | 最大事件间隔 3.60–4.17 s | 检查原生热迭代持有 GIL 的停顿，保持相同计算量与取消检查位置 |

- 数值候选先做局部试验；保留条件为受影响完整求解中位数至少改善 5%，且
  五次交错配对均显示同向收益。其他代表的求解与完整流程中位数不得退化超过 5%。
  某类无符合条件的合理局部改进时，必须给出热点、已存在的复用、试验/否决依据，
  单独记录该类结论；不能以 GUI 改善替代。
- 最终日常集以每个固定 ID 的 B0/C 各一次预热、各五次正常测量作交错比较，
  相邻 ID 轮换先运行的版本。初始 B0 五次用于本关口，最终补测 B0 用于控制
  电源/温度随长任务变化的影响，全部样本保留。若配对方向受波动影响，只允许
  再补一组完整五对；不选取有利子集。收益落在噪声内则终止该候选。
- 交互目标独立：上海 1/16 二维预热后，计算期间每次最大事件间隔均不超过
  1 秒、五次的中位数不超过 0.5 秒。按 B0/C/B0/C 块交错运行，每块先预热，
  两块分别取 2/3 次，共每版本每工况五次；正常桌面 20 次及取消/失败恢复另验。
- 不降低预算、停止标准、网格或输出。198 个固定成员全部重跑，并沿用公共
  集成的 `rtol=atol=1e-10` 比较 Q、两侧压降/温度、必要质量/能量诊断、状态、
  原生场和边界证据；任何新增不合格或结果回归阻塞合并。
- 新代码改变 JIT 入口时，用相同隔离缓存协议观察候选首次成本；三次启动仅
  报范围，不声称 p95。完成有意义的局部改善及四类独立结论后停止，参数优化、
  模型重标定和主要后端替换仍不进入本轮。

## P10：局部试验与取舍

四类数值效率独立完成剖析和局部试验；最终交错重复与不退化检查仍归 V00。
当前没有数值候选满足已冻结的保留条件，不把交互收益计为数值提速。

| 对象 | 实测及已有复用 | 本轮取舍 |
|---|---|---|
| sCO₂ 二维 | 主要耗时为焓到温度的 EOS 查询。现有 cp/k 已合并查询，耦合检查算出的温度已供下一轮复用；h/P 改变后必须重新求状态 | 16 组实际保存状态的 HP 合并 T/cp/k 查询局部快约 8%–12%，但 5 组冷侧 cp 超出 `1e-10` 一致性要求，不采用。继续保留现有状态检查和 EOS 口径 |
| sCO₂ 三维 | 3744 单元实际使用 AMG，层级已按漂移/周期复用；数值矩阵随 SIMPLE 更新，不能复用旧 LU。焓查询有同样的上述限制 | 初始双侧 SIMPLE 改串行，D8 求解中位数仅由 23.266 s 到 23.102 s，约 0.7%，不采用 |
| 上海二维 | 主要耗时为 LTNE（含必要 Richardson 加密检查）。Picard/SOU 每 sweep 的状态更新不能跨 sweep 缓存 | 固定四面展开及固定导热系数预计算两组试验均未改善完整求解，且原生残差比较有超限；均已撤销。性能、残差和原补丁保留 |
| 上海三维 | 600 单元实际使用直接 LU。改变的矩阵仍需分解；简单预处理/调度成本相对其不足半秒的总耗时不可忽略 | MMD 排序比现有 COLAMD 仅节省约 13 ms（3.3%/4.1%），未达 5%；串行初始双侧约慢 14%，均不采用 |

- 局部试验保存为 `P10-scheduling/`、`P10-lu-order/`、`P10-eos-state-probe.json`、
  `P10-unroll/`、`P10-coefficients/` 及对应补丁、比较和退出记录。
  前几组普通沙箱试验的 RSS 采样明确记录了 `ps` 权限失败；它们仅用于筛选，
  不替代最终具有有效 RSS/线程采样的正式比较。失败和首次编译均未删去。
- 上海 16 加密网格焓平衡残差曾相差 `1.60642832e-10 W/m`，超过冻结
  `1e-10` 容差。随后在完全未修改的 B0 独立空缓存上复现同一差异
  （`B0-cold-control-shanghai-16/`）；因此不能归因于 `nogil` 或展开试验。
  旧缓存重复可通过，不能证明新源码或空缓存通过。临时 GIL 包装方案已撤销。
- 根因是共享 `_model_h` 未显式指定 `fastmath`，Numba 会继承首次编译调用者
  的配置；旧缓存链接 fastmath 标量，新缓存链接 strict 标量。完整真实 LTNE
  内核的小网格调用顺序测试在修补前失败，5 个值最多相差
  `1.13686838e-13 K`；单独标量或面通量测试不足以复现链接后内联差异。
  显式 `fastmath=False` 后同一测试通过（`jit-order-chunk-before.log` /
  `jit-order-chunk-fixed.log`）。两处调用者模块同步更新说明，使已嵌入旧 IR 的
  Numba 磁盘缓存失效；不删除用户缓存，不变更物性公式。
- 确定性修补形成 B1 `262416b`，候选 C `4919639` 仅给现有二维串行内核
  添加 `nogil=True`。日常性能采用 B1/C 相同隔离编译条件和完整交错重复。
  数值参考保留不受该焓分支影响的 B0 166 项 sCO₂，仅补测 B1 上海 32 项；
  `final-reference/reference-index.json` 逐成员记录真实来源。候选 C 仍重跑
  完整 198 项，原容差、冷启动及交互目标均不变。sCO₂ 三维仍使用两次旧
  温度内核初始化，但其 `model_mass/cp=None`，不进入改变的共享焓分支；
  初始化算式和主焓内核未变。不能沿用旧计时替代新的 B1/C 性能对照。
  初始多排的 B1 sCO₂ 参考计算被主控停止，已完成行及中断原生退出 `-2`
  全部保留；这是避免重复参考计算的调度修正，不是求解器失败。
  B0、全部试验与失败记录保留，不将新参考冒充原 B0 或声称提高实验精度。
- 直接释放 GIL 的真实 Cocoa 初测，上海 1/16 二维最大事件间隔约 0.31–0.36 s；
  这是交互候选筛选证据。最终源码必须另行完成预定桌面序列及五次目标验收。

## V00：最终结果入口

2026-09-14 用户批准 sCO₂ 数值提速的补充范围：只在中间焓迭代使用
CoolProp BICUBIC 的 T(h,P)，最终温度、耦合能量检查、出口反算及其他物性
保留 HEOS。正式候选须对照固定 166 项 HEOS 参考：Q 与两侧压降变化
各不超过 0.1%，出口及完整温度场变化不超过 0.01 K；现有物理适用域、
状态拒绝、收敛、流量及能量守恒门槛独立保留。上海 32 项维持原 `1e-10`
比较。原严格差异报告仍输出，不将近似算法记作逐值相等或实验精度提升。
结果记录实际算法及 CoolProp 版本；每次求解独有状态，避免并发共享。
复用 CoolProp 自带磁盘表缓存，首次建表、已有磁盘表和同进程重复分别计时。
基准进程以 `COOLPROP_ALTERNATIVE_TABLES_DIRECTORY` 指向工作树 `.cache/`
目录（路径需以 `/` 结尾）；默认应用使用 CoolProp 原生缓存，不新增依赖。
原候选 C 的已完成验收保留，新候选补跑固定集和受影响的性能/可靠性检查。
两项既有混合流体回归的 Q 快照随该算法更新（不是误差容差放宽）：
sCO₂—水为 `45645.89445485597 W/m`（相对原值 +0.00005153%），
空气—sCO₂ 为 `4419.516474200189 W/m`（+0.000002398%）。仍按
`rtol=atol=1e-10` 比较新快照，保留两例原“不收敛”及原生/显示场分离断言；
原失败、原生结果及差异在本地 `E-mixed-reference/` 中保存。

PR #21 的最终说明分别记录：完整 198 项原生比较、四类日常求解/完整流程
交错计时、真实桌面数值资格与 GUI/公共接口一致性、五次事件间隔、首次与
预热成本、本地测试、独立审查及实际提交的必要 CI。运行命令退出 0 本身
不代替这些门槛。报告不将交互收益记为数值提速，不将软件资格记为实验精度。

本地 `final-acceptance.json` 由 `final_gate.py` 对上述实测应用固定标准；
`final-schedule-v2.json` 保留逐阶段原生退出，`final-reference/reference-index.json`
保留逐成员的 B0/B1 参考来源。所有失败和调度中断另有原始记录。
合并状态与主分支提交以 PR 的实际 GitHub 状态和最终本地回读为准。
本阶段不改写原三模块 M-A/M-B 状态。
