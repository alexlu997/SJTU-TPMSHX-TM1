# M-A 架构验收：完成

2026-09-11，PR #7 已正常合并至独立 TM1 main：
`8ad9a55e9f0354d203337021903be5eaaab09b3b`。
最终候选 `1f9bc07ee3f49565b250d7cc638ec4005024def4` 与合并结果文件树一致；
候选与 main 的矩阵及最小环境 CI 均成功，合并后真实交接/来源 smoke 4 项通过、原生退出 0。
37 个 M-A 节点完成；8 个 M-B 节点保持原状态。
最终证据见 [合并后验收](evidence/Z00/post-merge-verification.md)。
各节点以 state/*.json 为准；本表不是另一状态真源。
B40 按批准范围完成四项技术处置与发布验收，原 4/4 失败及所有后续历史保留。
这不宣称旧参考通过、整机实验精度通过或所有模式均达到工程收敛。

2026-09-11 后续结构整理已通过 [PR #9](https://github.com/alexlu997/SJTU-TPMSHX-TM1/pull/9)
正常合并为 `c129695fabfaf29595cb375f95d79025cc821005`，包括矩形计算边界与首次运行说明、
统一结果映射、直接导入和二维运行控制。该 PR 记录独立审查、实际测试候选与合并后检查；
合并后的两个 CLI 示例原生退出均为 0。M-A/M-B 的验收范围与下述历史记录保持原有边界。

## 合并前需求核对与历史审查记录

以下记录保留各阶段的限制和当时待办；最终发布状态以上述合并后证据为准。

| 节点/要求 | 可核查实现或证据 | 当前限制 / 下一项 |
| --- | --- | --- |
| G00 接管与协调 | TAKEOVER.md、独立 worktree/提交历史 | 最终目标分支和 PR/CI 状态需实时复核 |
| G10 公共契约 | domain 的 CaseData/FieldResult/RunControl；schemas/three_module_v1 | 最终审查；不能携带回调或活动求解器对象 |
| B20/B30 参考基线 | evidence/B20、B30 的选择记录及 S20/S30 真实比较 | 原历史 NaN、未收敛、缺 native exit 不回填成成功 |
| B40 旧优化锁定值 | 历史归因、工程预算、固相诊断与批准的 reference-disposition-proposal.md | 原 4/4 失败保留；批准更新后的四项回归通过，整体 B40 仍未解除 |
| M00 共享模型 | models/；evidence/M00/extraction.md；导入分层检查 | 后端不得重复闭合，物理适用域不因提取放宽 |
| P20/P30 前处理 | preprocess/two_d、three_d；prepared-flow / prepared-thermal 证据 | 固定几何、边界、系数交接；实际接收输入需校验 |
| P40 离线资源 | evidence/P40；显式源路径、拟合和本地发布 | 当前水 CFD 工作簿缺失；不替换生产表或原始数据 |
| S20/S30 数值执行 | Python 2D/3D prepared runtime；真实集成证据 | 数值状态与独立物理验收分开 |
| S40 设计/筛选模式 | evidence/I54、S40；快速设计准备压损补充 | 保留模型近似及历史 3D 未收敛/退出 2；新焓模型原预算 legacy 收敛/退出 0 |
| R20/R30 原生证据 | result capture 与源场/通量/压力对比 | 显示字段不能代替原始温度和压力 |
| H10 工程指标 | postprocess/metrics.py 与分模式 reducer | Q/Tout/ΔP 的定义和不支持原因随模式保留 |
| D10/D20 正式文件 | YAML/HDF5/严格 JSON，evidence/V20/files.md、result-declarations.md | 文件单位和维度冲突已修复，24 项文件/真实交接回归通过；保留诊断 NaN |
| H20 离线可视化/导出 | postprocess/export.py；真实 2D/3D 求解结果在独立进程逐字段 VTK 回读 | 本地 2 项通过、原生退出 0；0ba1657 真实集成 CI 成功 |
| E00 环境 | 主锁、最小后处理锁和 CI 环境检查 | 代码候选 0ba1657 三模块及 macOS/Windows 矩阵均成功 |
| Q10 测试/示例入口 | 分层测试、examples/ 参数及有效场调用 | slow/heavy 不在快测覆盖内 |
| A10/A20 应用编排 | 公共 CLI 和控制器适配；evidence/A20 | UI 和报告消费结果数据，不读取运行时私有对象 |
| A30/A40 外部优化/设计 | 公开 Case/Result 调用；参数、场及模式接口 | 无虚构梯度或伴随实现 |
| I20/I30 真实全模型集成 | integration_tm1 与 S20/S30 证据 | 本地真实结果通过不代替最终远端验收 |
| I40 模式集成 | evidence/I54、S40；跨进程筛选/设计测试 | 保留各模式的能力/拒绝范围 |
| I51 CLI / I52 GUI | A20 接线与真实应用映射 | 0ba1657 真实集成 CI 成功；合并后验证未完成 |
| I53 优化 / I54 设计 | 原生数组同源比较与三进程指标 | 2D/3D 积分焓及参考更新已批准并验证；历史边界缺口保留，最终综合验收待完成 |
| I55 依赖边界 | 实际 prepare/solve/postprocess 子进程导入检查 | 最小环境真实文件求值须由 CI 实际执行 |
| I56 报告/应用交接 | HDF5 后独立进程重建 ComputeResult | 不从旧摘要或 GUI 状态重算指标 |
| V20/V30 文件边界 | 输入文件分阶段删除后的真实独立进程测试 | 完整文件与状态证据，不以临时 NPZ 替代正式交接 |
| V40 隔离/最小运行 | 真实同进程 2D/3D 并行与取消、警告隔离通过 | 最小环境 CI 34549895031 已通过；仍须完整独立审查 |
| Z00 最终收口 | 本表、README、architecture 和原始历史存档 | 独立审查、必要 CI、基线决定与合并后验证未齐 |

## 合并前发布门槛记录（现已按合并后证据完成）

1. 四项漂移已获受控归因；2D/3D 筛选积分焓修复及对应参考更新均已明确批准。四项冻结、四项三进程交接与两项 3D 焓边界验证共 10 项通过，14.27 s、原生退出 0。质量、2D 参考及容差不因最后两项 3D Q/dP 更新而改变。原始 4/4 失败、后续失败和旧值保留。3D 原生积分焓历史差额 3.67–5.02% 保留；新模型六面质量积分焓、入口导热和相间换热闭合证据见 `evidence/B40/3d-model-h-validation.md`。原预算 legacy 收敛不等于 F2 资格，独立工程预算证据另列。整体 B40 尚待综合验收，不增加四个筛选案例整机实验要求，也不宣称实验精度通过。
2. 完整实现审查发现的来源 P2 已修复并独立复核；最后参考更新 `0ba1657` 相对 `dcb0bf8` 的五文件差异亦经只读独立复核，未发现具体问题，确认旧值、失败及状态记录保留。审查未额外重跑 PDE。
3. 精确候选的既有 macOS/Windows CI、真实集成以及 Linux 最小环境运行。
4. 目标 main 的基线一致性、串行正常合并及合并后验证。

M-B 的原文能力和未实现项另见 acceptance_document.md。架构完成不得推导
sCO2 Q 准确性、旧 B40 通过、未运行物理验证或跨后端等价性。

2026-09-11 独立有限复查（`b40_review`，源 `5d0a686`）：公开全模型
`prepare_case → run_case → evaluate` 的 2D/3D 接点未发现读取
config_snapshot 重跑旧 pipeline、忽略准备数组或后处理调用求解器对象。
两维 execution 消费 Case 参数、网格、设计场和模型引用；runtime 消费准备的
流动/几何数组；capture 保存原生面、压力和状态；后处理只读取 FieldResult。
正式结果 loader 和直接 evaluate 入口均使用共享声明校验。此为静态数据消费
复查，未重新运行 PDE，也未逐行审查全部数值内核，不能代替全 PR 审查。

后续完整实现审查（源 `5d83501` 对 PR base `c7f0138`）检查前处理、模型解析、
实际消费链、捕获、持久化、独立后处理、运行控制和 GUI 桥接，发现 P2：结果
缺少实际代码/数据来源。修复在四个公开准备入口和统一求解入口记录分阶段来源，
保留源修订、tracked-change 状态、实际数据仓库修订与独立声明 pin；无 Git 明示
unavailable。真实 YAML/HDF5 交接与缺仓库检查共 2 项通过、原生退出 0
（`.cache/provenance-handoff.log`），独立复核确认该 P2 已修复。此来源记录不声称
能重建未提交改动或逐个输入文件的使用情况。B40 参考决定和最终 CI 仍单列。

来源修复后的公开 API、真实全模型三进程与来源交接合并验证：6 项通过，
24.33 s，原生退出 0（`.cache/provenance-public-integration.log`）。
此前提交 `797e4ec` 的矩阵 CI `34559970376` 与三模块 CI `34559970329`
均已成功；后续 3D 模型与来源修复仍需当前提交的 CI，不能继承旧绿色结论。

固定 166 项真实实验迁移对比已完成，两端原生退出均为 0。全部配置、
收敛状态及公共诊断一致；Q、热侧压降和两侧出口温度逐项一致，冷侧压降
最大差异 5.820766091346741e-11 Pa。四组实验 Q RMSRE 保持原值；
完整固定分母及源提交见 `evidence/real-experiments/fixed166-comparison.json`。
该结果证明记录源版本的迁移保真，不构成实验准确性或全边界能量验收。

CI 历史：`77d694b` 的矩阵 `34562270740` 因测试模块重名在收集阶段失败；
重命名后两个来源测试模块合并验证 8 项通过、原生退出 0。`dcb0bf8` 的
三模块 `34562815932` 成功，矩阵 `34562815803` 被后续推送取消，不能记为通过。

2026-09-11 代码候选 `0ba165768b66eba7b19f29b2e783f6afec78fb69`：
矩阵 CI `34563788328` 成功。macOS/Windows 快测均为 2979 passed、
20 skipped、98 deselected；真实公共模块集成均为 27 passed、1 skipped。
三模块 CI `34563788330` 成功。PR #7 为 OPEN、draft、MERGEABLE，
base 为 `c7f013869e7127b35ac0c305376c13b3a834131b`，尚未合并，
GitHub reviewDecision 为空。不能把可合并或 CI 成功视为最终验收完成。
