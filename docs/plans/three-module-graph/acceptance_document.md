# V0.1 / M-B 能力追踪：未完成

依据：V01_SOURCE.md 与 REQUIREMENTS.md，保留原文“可选、逐步、可扩展、
必要时、仅预留”的限定。此表记录当前实现边界，不改变需求优先级，也不代替
各节点的状态文件。M-A 本身仍未完成，Z10 不具备收口条件。

| V0.1 要求 | 当前可核查范围 | 尚未交付或待验收 |
| --- | --- | --- |
| 前处理、求解、后处理独立 | 三个公开 API；CaseData/FieldResult；真实分进程证据 | 最终独立审查、CI、合并验证和 B40 基线决定 |
| 统一工作流和外部应用调用 | GUI/CLI、设计、优化、参数/有效场示例接线 | 最终代码组合的远端验收 |
| Python/Numba 路线 | 实际全模型 2D/3D、筛选及快速设计 | 数值/能量/实验结论按模式分别保留，不由架构通过推导 |
| Native C/C++ 后端 | 公共求解控制和结果接口可作为接入边界 | X10/X11 未实现真实后端，无跨后端数值证据 |
| OpenFOAM 适配 | 公共物理数据与后端隔离 | X20/X21/X22 未实现真实求解/转换/交叉验证 |
| 其他 External 后端 | 非 Python backend 当前明确拒绝 | 无具体外部实现；不能将接口声明当成已支持 |
| 宏观几何与可选微观几何 | 当前 TPMS 固定/分区几何准备，共享单胞几何计算 | 不宣称宏观求解解析真实微观曲面；可选显式微观网格流程未交付 |
| 网格生成/导入、边界与材料 | 当前正交网格和 Case 文件回读；实际开口、侧别、方向、设计场 | 不宣称任意非结构网格或新格式可直接求解 |
| 实验/CFD/DNS 清洗拟合 | 显式离线入口，现有实验和 CFD 减缩、Nu/DF 拟合 | 当前水 CFD 工作簿缺失；没有独立 DNS 数据证据 |
| 均质化参数计算、读取、插值 | Case 设计场、固定几何和版本化 CFD 表；模型支撑独立 | 不宣称已实现任意方向性均质化模型 |
| 后端无关均质化中间层 | 公共 SI 网格、物理场、边界和资源描述 | C++/OpenFOAM 的实际映射仍需对应后端验收 |
| 方向性参数优先张量表达 | 当前既有标量/方向字段与明确轴语义 | 通用张量参数契约和各向异性离散未交付，X30 继续跟踪 |
| 物性与控制方程解耦 | models 中共享空气、水、sCO2 物性与相关式 | 新提供器必须实际接入，不将任意 callable 放入持久化 Case |
| 常物性提供器 | 快速设计可在单次热迭代固定所评估物性 | 尚非通用可配置常物性提供器 |
| 表格物性提供器 | 现有闭合参数表并不等于流体物性表 | 通用物性表适配器未交付 |
| 拟合物性 | 现有空气/水公式由单一共享模型提供 | 通用用户拟合资源注册/跨后端执行未交付 |
| CoolProp / sCO2 T-P 强变物性 | 现有实际状态评估、焓/物性路径和状态检查 | 适用域、关联式证据和实验 Q 验收仍分别受限 |
| REFPROP / 用户模型 | 未见可运行的对应适配器 | X30 未完成；许可和运行库状态未在此表核验 |
| FVM 内部职责 | 现有 SIMPLE/LTNE、边界、离散和收敛内核保留 | 不能据 Python 内核推导 C++ 或外部后端等价 |
| Darcy→DF→各向异性，LTE→LTNE | 现有 DF/LTNE 能力保留 | 原文逐步能力单独验收，不重写已用物理模型凑清单 |
| Q/ΔP/Tout | 从原生证据计算；单位/支持状态随模式定义 | 未定义或缺证据的模式返回原因，不虚构指标 |
| h/Re/Nu/f/PEC | 快速设计已有 Re；核心模型有局部闭合输入/输出 | H30 仍需明确 h 面积/温差、f 约定、PEC 比较基准和所需数据 |
| 场读取、可视化、报告 | HDF5 回读、真实 2D/3D 逐字段 VTK 回读、离线指标和 GUI 映射 | 最终最小依赖 CI 仍需实际运行 |
| YAML/HDF5/VTK/JSON | 正式实现和分层读写测试 | 最终 CI 证据未取得 |
| 所有内部 SI / schema_version | 新持久化合同 SI、模式 schema；旧叶内核显式 mm 适配 | 原文“全部内部 SI”尚未完成，X30 需逐项迁移并验证 |
| pybind11、必要时 C API | 原文允许的接入方式 | 没有真实 C++ 后端时不虚构绑定或稳定 ABI |
| 优化仅改变公开设计/均质化场 | 支持范围内 Case 设计场输入，不改私有矩阵 | 更广泛张量/任意三维场须先有实际模型支持 |
| 第一阶段仅预留梯度/伴随 | 不返回虚构梯度；未实现状态明确 | 保留原文阶段限定，不宣称伴随可用 |
| DOE/GA/PSO/NSGA-II/贝叶斯/梯度/代理可扩展 | 既有优化应用继续接公开 API | 未实现算法不声称支持，保留“可扩展”限定 |
| 测试、示例和文档 | 现有分层测试、公开调用示例和本追踪表 | 目录和测试数量都不能替代实际验收 |

H30 的未定定义只阻塞相应扩展指标；不能临时选择一种平均 h、摩擦因子或参考
换热器来填数。M-B 缺项继续由 H30、X10/X11、X20/X21/X22、X30 与 Z10 追踪。
本任务的 M-A 架构完成也不会将此表改成 V0.1 全功能通过。
