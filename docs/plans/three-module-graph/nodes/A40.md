# A40｜换热器设计软件的公共调用适配

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/a40`。
状态记录：`state/A40.json`。

## 任务

1. 保留尺寸搜索、设计规则和界面在调用方；数值部分通过公开的模型模式与 Solver 调用。
2. 给定速度场+解析压降的设计模式不能隐式换成完整 SIMPLE，也不能反向把完整模型降级。
3. 用契约端口验证调用和返回映射，旧 design.forward/sizing/CLI 接线留 I54。

## 独占写入范围

- `sjtu_tpmshx/design/public_forward.py`
- `examples/three_module/external_design.py`
- `sjtu_tpmshx/tests/apps_tm1/test_design_adapter.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 原有输入输出约定、模型身份和预算可被显式表达，无私有内核调用。

## 必需证据

- 应用接口测试、模式映射和示例

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
