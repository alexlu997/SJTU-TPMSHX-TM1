# H10｜核心工程指标与独立后处理

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/h10`。
状态记录：`state/H10.json`。

## 任务

1. 从 FieldResult 独立计算既有 Q、ΔP_A/B、Tout、质量及守恒诊断，保留每条模式原有定义。
2. 二维 Tout 按原始出口面正向外流质量加权，不乘 cp，不读显示/外推温度，保留有符号通量供守恒判断。
3. 相同算法确需供在线监控使用时放入只接受数据的共享纯函数，不让求解模块导入后处理包。
4. 缺少证据返回 unsupported/insufficient_data，不自动求解或反推缺失局部场；结果有效性与程序运行状态独立。

## 独占写入范围

- `sjtu_tpmshx/postprocess/metrics.py`
- `sjtu_tpmshx/postprocess/diagnostics.py`
- `sjtu_tpmshx/result_math.py`
- `sjtu_tpmshx/tests/postprocess_tm1/test_core_metrics.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `sjtu_tpmshx/pipelines/solve_2d.py::_outlet_temperature_2d`
- `sjtu_tpmshx/pipelines/run_stack_3d_stages.py::_extract_3d_metrics`

## 验收

- 能用人工场核对公式、单位深度、反向流和局部开口；只读指标标量照抄不算通过。
- 不导入 SIMPLE/LTNE、GUI 或活动求解器；真实输出比对由 I20/I30 完成。

## 必需证据

- 指标口径规格、独立算例测试、依赖测试

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
