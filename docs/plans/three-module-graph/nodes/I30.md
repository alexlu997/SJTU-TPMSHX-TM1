# I30｜3D 真实三模块接线与旧路径去重

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10, B30, M00, P30, S30, R30, H10。
分支：`codex/tm1/i30`。
状态记录：`state/I30.json`。

## 任务

1. 唯一有权修改本维度旧流水线的节点。连接真实模型、前处理、执行器、结果提取器和指标计算。
2. 将旧路径变成委托或删除重复实现；保留已发现且仍有调用者的兼容入口，明确退役位置，不新造永久兼容框架。
3. 保持实际网格、坐标、孔隙率、质量通量、压力、热状态、粗细网格与收敛规则；物理修正另外立项。
4. 运行 B30 的真实回归，检查数值与状态而不仅看类型；发现缺字段回送 G10 的定向契约变更，不私自扩 schema。

## 独占写入范围

- `sjtu_tpmshx/pipelines/stages_3d.py`
- `sjtu_tpmshx/pipelines/run_stack_3d.py`
- `sjtu_tpmshx/pipelines/run_stack_3d_stages.py`
- `sjtu_tpmshx/pipelines/grid_3d.py`
- `sjtu_tpmshx/pipelines/flux_3d.py`
- `sjtu_tpmshx/pipelines/stages_3d_helpers.py`
- `sjtu_tpmshx/workflows/integration_3d.py`
- `sjtu_tpmshx/tests/integration_tm1/test_3d_real.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 内存方式真实三模块链通过，前处理退出/失效不影响已准备输入的求解。
- 没有执行旧完整 Pipeline 的隐藏回路；正常求解不调用正式后处理。
- 受影响真实基准及原测试通过，日志包含精确提交与实际环境。

## 必需证据

- 真实基线比较、测试日志、重复实现清理清单

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
