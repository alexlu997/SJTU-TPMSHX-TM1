# P40｜离线数据处理与模型发布职责整理

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10, M00。
分支：`codex/tm1/p40`。
状态记录：`state/P40.json`。

## 任务

1. 保留 V0.1 的实验/CFD 数据清洗、拟合与模型发布职责，普通 Case 构建不能强制重拟合。
2. 把已存在的离线入口按职责明确转接或归位；固定数据版本和输出模型来源，原始测量继续保存在既定本地位置。
3. 人工小数据可测 I/O 与拟合管线行为；真实原始数据缺失则对应真实复验标记未运行，不改变生产闭合来通过。

## 独占写入范围

- `sjtu_tpmshx/preprocess/offline/**`
- `sjtu_tpmshx/tests/preprocess/test_offline_tm1.py`
- `docs/plans/three-module-graph/decisions/offline_models.md`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 离线准备与在线模型评估有独立入口和资源边界；普通前处理不依赖原始实验工作簿。
- 未提交原始数据，模型发布与生成报告不变成新的原始测量真源。

## 必需证据

- 离线流程测试、资源/来源说明、真实数据缺口清单

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
