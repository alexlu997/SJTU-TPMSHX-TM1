# D10｜Case YAML/HDF5 读写与旧配置映射

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10, E00。
分支：`codex/tm1/d10`。
状态记录：`state/D10.json`。

## 任务

1. 实现 case.yaml 与 case.h5，保留 JSON 配置入口；字段格式严格按 G10，不重新发明隐含默认值。
2. 校验 shape/dtype/单位/版本/模型资源/边界，采用安全解析；禁止 pickle 或执行任意 Python 对象。
3. 把本机绝对路径、环境变量覆盖等解析为明确资源与实际配置记录；不可恢复的依赖明确拒绝。

## 独占写入范围

- `sjtu_tpmshx/io/case_io.py`
- `sjtu_tpmshx/io/yaml_config.py`
- `sjtu_tpmshx/tests/io_tm1/test_case_io.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 新进程可读写人工 CaseData；字段/数组与状态语义往返一致。
- 格式名与真实格式一致；只用 NPZ 不能标记 HDF5 完成。

## 必需证据

- 文件样例、损坏/未知版本测试、依赖证据

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
