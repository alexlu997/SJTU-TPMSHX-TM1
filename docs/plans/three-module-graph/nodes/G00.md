# G00｜执行基线、授权与所有权登记

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：无。
分支：`codex/tm1/g00`。
状态记录：`state/G00.json`。

## 任务

1. 读取当前 AGENTS、README、architecture；发现已有 Goal/Graph 时将本计划映射进去，保留活动节点、证据和授权，不并存两套事实状态。
2. 重新取得 origin/master、实际分支、未提交修改、活动 PR、worktree 和任务；本计划参照 5f1cafb，执行时新增提交只触发受影响节点复核。
3. 登记用户本轮自主提交、推送、PR、CI、合并授权；仅限本 Goal，不改仓库权限，不动无关数据与任务。
4. 建立节点所有权、共享文件队列、资源预算与调度状态；计划文件 bootstrap 不要求先完成数值环境检查。

## 独占写入范围

- `docs/plans/three-module-graph/**`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 执行基线与计划参照基线分别记录；来源不明的脏改动没有被清理或提交。
- 每个共享文件有唯一当前写入方；节点 ID 与本地现有图映射明确。

## 必需证据

- 基线 SHA、活动任务清单、授权记录、所有权登记

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
