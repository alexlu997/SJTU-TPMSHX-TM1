# Z00｜架构里程碑 M-A 收口

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：I51, I52, I53, I54, I55, I56, P40, V20, V30, V40, H20。
分支：`codex/tm1/z00`。
状态记录：`state/Z00.json`。

## 任务

1. 逐项核验现有能力覆盖、两个交接点、三模块独立性、外部无侵入调用与依赖边界。
2. 整理接口文档、旧入口去留和已实现/未实现能力表；目录示意不得描述成超范围功能。
3. 检查最终主分支的 CI 和真实回归，不直接合计节点单元测试作为全链验收。
4. M-A 不等待未承诺实现的伴随或任意各向异性；不把 M-A 当作全文档或多后端完工。

## 独占写入范围

- `docs/architecture.md`
- `README.md`
- `docs/plans/three-module-graph/acceptance_architecture.md`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- M-A 必需证据齐全且最终代码一致；缺失项保持未完成。

## 必需证据

- 架构验收表、最终合并提交、最终 CI、已知限制

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
