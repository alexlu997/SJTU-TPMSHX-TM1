# B20｜2D 当前行为基线与回归清单

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G00。
分支：`codex/tm1/b20`。
状态记录：`state/B20.json`。

## 任务

1. 为当前 2D 主计算链选取既有小算例，覆盖支持的流体、方向、局部端口、分区/偏移与错误路径；不笛卡尔积盲目扩大测试。
2. 记录实际网格、物理模式、求解预算、输出状态与关键量；现有拒绝的组合仍应拒绝。
3. 基线数据在合法且匹配锁的环境生成；环境/原始数据缺失仅阻塞本节点的真实验证，不要求其他静态节点停止。
4. 不修改内核、系数或通过阈值。历史物理精度与当前纯重构回归基线分开。

## 独占写入范围

- `sjtu_tpmshx/tests/baselines/tm1_2d/**`
- `docs/plans/three-module-graph/evidence/B20/**`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `sjtu_tpmshx/tests/`
- `sjtu_tpmshx/validation/`

## 验收

- 关键输出有可追溯参考和比较方法；无测试结果不能用 expected pass 代替。
- 执行前确定比较容差并记录来源；不以源码哈希或快照摘要代替行为测试。

## 必需证据

- 基线 SHA、测试配置、环境/模型版本、输出和未运行清单

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
