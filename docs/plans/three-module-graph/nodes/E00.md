# E00｜CI、依赖声明与隔离运行环境

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G00。
分支：`codex/tm1/e00`。
状态记录：`state/E00.json`。

## 任务

1. 保留当前 macOS/Python3.13 与 Windows/Python3.12 的锁检查、pip check 和 not slow/not heavy 测试。
2. 为 HDF5/YAML 必需依赖作最小声明与锁更新；先检查是否已有安装和复用实现，不扩展无关依赖。
3. 规划共享核心、Python后端、I/O、可视化、测试的最小安装边界；新隔离 CI 环境按仓库工作流创建，本地共享 venv 不自动重建。
4. 增加三模块测试任务入口与稳定验收检查；CI concurrency 按 PR/分支隔离，矩阵 fail-fast=false；禁止全仓库单一 cancel-in-progress 队列。
5. 未配好数据或后端的验证明确显示 blocked/not-run，不将 skip 当作该能力通过。若使用 GitHub merge queue，另加 merge_group 触发并验证，不能仅声明支持。

## 独占写入范围

- `.github/workflows/ci.yml`
- `.github/workflows/three-module.yml`
- `pyproject.toml`
- `requirements-lock.txt`
- `requirements-lock-server.txt`
- `mypy-core-files.txt`
- `sjtu_tpmshx/runs/tools/check_three_module_environment.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `.github/workflows/ci.yml`
- `pyproject.toml`
- `AGENTS.md`

## 验收

- 新增依赖声明与适用锁一致，现有检查不被删除或宽松化。
- 独立 CI 环境能够运行相应 I/O/后处理测试；共享开发环境没有被悄然改动。
- 检查清单可被合并协调者读取；不因 GitHub combined status 无条目就判断成功。

## 必需证据

- CI run/job URL、锁检查结果、依赖变更说明
- 必需检查列表与跳过项处理

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
