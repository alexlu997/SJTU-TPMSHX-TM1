# H30｜h/Re/Nu/f/PEC 扩展指标与数据需求

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：document_capability。
依赖：G10。
分支：`codex/tm1/h30`。
状态记录：`state/H30.json`。

## 任务

1. 逐项追踪 V0.1 明确列出的工程指标，先复用已有受支持定义。
2. 对 h 的面积/温差定义、f 的约定、PEC 的比较基准等未给出的物理选择，单独记录需确认项，不把任意定义包装成文档要求。
3. 按 MetricSpec 明确所需字段和参考数据；无法计算的项返回原因，不能为了完成清单填数。

## 独占写入范围

- `sjtu_tpmshx/postprocess/extended_metrics.py`
- `sjtu_tpmshx/tests/postprocess_tm1/test_extended_metrics.py`
- `docs/plans/three-module-graph/decisions/extended_metrics.md`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 已明确的指标有可验证定义、单位和测试。
- 物理定义未定时本节点/相应子项 blocked_scope，核心 Q/ΔP/Tout 及架构主线不停止。

## 必需证据

- 逐指标需求表、公式依据与测试、未定事项

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
