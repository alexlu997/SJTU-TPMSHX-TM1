# V30｜3D 三进程文件交接验收

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：I30, D10, D20, Q10。
分支：`codex/tm1/v30`。
状态记录：`state/V30.json`。

## 任务

1. 进程1构建并保存 Case 后退出；进程2只读 Case 求解并保存 Result 后退出；进程3只读 Result 重新计算指标。
2. 对照相同案例的内存链；检查网格、模型资源、状态时间点、单位、通量、警告及结果有效性。
3. 模拟上游对象不可用、原临时目录消失、接收进程环境变量不同；不允许重新解释已固定的物理设置。

## 独占写入范围

- `sjtu_tpmshx/tests/integration_tm1/test_3d_three_process.py`
- `docs/plans/three-module-graph/evidence/V30/**`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 三进程真实运行且关键值/状态一致；中途没有重跑前处理或求解。
- 需要的 HDF5/YAML 与结果资源实际可读，skip/not-run 不算通过。

## 必需证据

- 三个进程日志、产物与内存/文件差异报告

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
