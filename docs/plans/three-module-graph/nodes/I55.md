# I55｜旧模型入口委托、去重与初始化边界

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：M00, I20, I30, I40。
分支：`codex/tm1/i55`。
状态记录：`state/I55.json`。

## 任务

1. 只在新模型与数值链已验证后，将旧模型入口明确委托至单一真实权威，删除重复定义而非长期两份并存。
2. 拆开包导入与求解线程/JIT初始化；保留实际求解时明确执行线程配置的语义。
3. 需要兼容的旧名字保留单向委托并列清调用方与退役计划；不对未知调用者建立泛化 fallback。

## 独占写入范围

- `sjtu_tpmshx/solvers/fluid_props.py`
- `sjtu_tpmshx/solvers/tpms_props.py`
- `sjtu_tpmshx/solvers/nu_correlations.py`
- `sjtu_tpmshx/solvers/tpms_calc.py`
- `sjtu_tpmshx/solvers/__init__.py`
- `sjtu_tpmshx/tests/integration_tm1/test_model_authority.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 模型标量/数组输出与当前基线一致，状态检查和告警不丢失。
- 前处理与后处理导入路径不启动数值后端；完整独立安装由 V40 再验证。

## 必需证据

- 单一权威检查、模型回归、初始化/线程测试

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
