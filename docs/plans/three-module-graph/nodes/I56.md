# I56｜独立 CLI 与完整工作流命令接线

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：I51, A10, D10, D20。
分支：`codex/tm1/i56`。
状态记录：`state/I56.json`。

## 任务

1. 增加独立 prepare/solve/postprocess 和完整流程命令；保留已使用的旧 CLI 参数入口或明确兼容映射。
2. 输出真实文件包与状态退出码，不以日志摘要冒充 FieldResult。
3. 同进程调用默认不强迫每一步落盘；文件模式使用 D10/D20。

## 独占写入范围

- `sjtu_tpmshx/cli.py`
- `sjtu_tpmshx/workflows/cli_commands.py`
- `sjtu_tpmshx/tests/integration_tm1/test_public_cli.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 每个子命令可以单独在新进程执行；完整流程与分段流程一致。

## 必需证据

- CLI 真实调用、文件回读、退出码回归

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
