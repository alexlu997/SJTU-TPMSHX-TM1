# H20｜通用可视化、VTK 导出与报告

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10, E00。
分支：`codex/tm1/h20`。
状态记录：`state/H20.json`。

## 任务

1. 以 FieldResult/PerformanceResult 为输入实现绘图、报告、results.vtk 或经过明确映射的 VTK 系列格式。
2. UI 控件留在应用；导出不读 window._result 等私有状态，不调用求解。
3. 展示重采样不覆盖原生求解数据；警告、未收敛与外推信息进入报告。

## 独占写入范围

- `sjtu_tpmshx/postprocess/visualization/**`
- `sjtu_tpmshx/postprocess/report.py`
- `sjtu_tpmshx/postprocess/export.py`
- `sjtu_tpmshx/tests/postprocess_tm1/test_exports.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 人工结果在无求解后端的进程可导出；导出文件可以重新读取，场位置和单位不丢失。
- 报告标识精度等级和状态；真正整机报告验收另在集成节点执行。

## 必需证据

- 导出回读测试、代表报告与绘图检查

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
