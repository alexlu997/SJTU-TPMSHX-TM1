# R20｜2D 结果证据提取器

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/r20`。
状态记录：`state/R20.json`。

## 任务

1. 把后端内部数组转成 FieldResult，保存原生场、边界通量、压力提取依据和状态快照。
2. 分别标明最后热求解状态、最终报告状态、显示/外推场；不要用不同状态的 T/P/质量流量拼出一个无标识结果。
3. 二维保留 PR93 的原始出口温度与最后热输入质量流量依据；三维保留已有边界/通量记录及压力口径。
4. 利用冻结运行状态样例独立开发，不等待 S20 的实际运行实现；不改旧 stages 文件。

## 独占写入范围

- `sjtu_tpmshx/solvers/backends/python/two_d/result_capture.py`
- `sjtu_tpmshx/tests/solver_tm1/test_capture_2d.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `sjtu_tpmshx/pipelines/stages_2d.py`
- `sjtu_tpmshx/pipelines/solve_2d.py`

## 验收

- 字段单位、坐标、开口与流向映射正确，通量带符号且没有再次乘面积或孔隙率。
- 保存足够数据供 H10 独立计算，不仅转抄 Q/ΔP；真实一致性由 I20 验收。

## 必需证据

- 字段映射、状态时间点说明、人工样例测试

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
