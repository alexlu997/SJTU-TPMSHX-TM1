# V40｜最小依赖、并行隔离及维护边界验收

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：I20, I30, I40, E00, Q10, D20, H10, H20, I55。
分支：`codex/tm1/v40`。
状态记录：`state/V40.json`。

## 任务

1. 在合规的最小依赖 CI 环境验证后处理，不安装/导入数值后端和 Qt；需要的绘图/模型依赖单独声明。
2. 验证不同 Case 并行运行时警告、缓存、取消、配置和结果不相互污染，CaseData 不被回写。
3. 同时做静态依赖与真实导入/运行测试；共享数据和模型支撑不成为另一隐藏完整流水线。

## 独占写入范围

- `sjtu_tpmshx/tests/integration_tm1/test_minimal_install.py`
- `sjtu_tpmshx/tests/integration_tm1/test_state_isolation.py`
- `docs/plans/three-module-graph/evidence/V40/**`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 所有已声明独立运行组合有实际证据；环境没建立不得写测试通过。

## 必需证据

- 最小安装清单、隔离测试和并行资源配置

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
