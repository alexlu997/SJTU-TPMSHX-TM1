# S30｜3D Python 求解执行器

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/s30`。
状态记录：`state/S30.json`。

## 任务

1. 只消费 CaseData，建立自己的运行状态、初次求解与后续 SIMPLE—LTNE 耦合；运行控制单独传入。
2. 保留数值内核及迭代次序，显式使用最终网格，不回调前处理重新建问题，不调用正式后处理。
3. 按冻结接口注入模型提供器与结果提取器。单元测试可使用替身验证调用次序与异常，不声称已完成真实闭环。
4. 无论是否随后进行正式后处理，数值收敛、状态有效性、取消和错误判断都必须正确。

## 独占写入范围

- `sjtu_tpmshx/solvers/backends/python/three_d/execution.py`
- `sjtu_tpmshx/solvers/backends/python/three_d/runtime.py`
- `sjtu_tpmshx/tests/solver_tm1/test_execution_3d.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `sjtu_tpmshx/pipelines/run_stack_3d_stages.py`

## 验收

- 接口消费的是准备好的算例，而不是内部调用旧完整 Pipeline。
- 不修改输入数组/嵌套对象；取消与真实异常不混淆；真实算例验收留给 I30。

## 必需证据

- 运行控制/不可变性测试、数据消耗清单

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
