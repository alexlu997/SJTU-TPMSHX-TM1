# S40｜筛选与快速设计的数值模式归位

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/s40`。
状态记录：`state/S40.json`。

## 任务

1. 把既有筛选/快速设计中的数值实现按准备与执行两部分归位，独立模式分别维护，不复制一个影子完整流水线。
2. 同一工作节点跨两个业务目录是因迁移源的一次性原子拆分，仍需遵守准备不执行宏观求解、求解不改物理网格。
3. 保留给定流场、解析压降、低预算、warm start 等原始含义；输入通过标准 CaseData。
4. 旧 core/evaluators、design/forward 文件只读，最终委托与去重由 I53/I54 完成。

## 独占写入范围

- `sjtu_tpmshx/solvers/backends/python/screening/**`
- `sjtu_tpmshx/solvers/backends/python/quick_design/**`
- `sjtu_tpmshx/preprocess/app_modes/**`
- `sjtu_tpmshx/tests/solver_tm1/test_app_modes.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `sjtu_tpmshx/core/evaluators.py`
- `sjtu_tpmshx/design/forward.py`
- `sjtu_tpmshx/optimization/evaluator.py`

## 验收

- 模式单元测试可用冻结样例完成；真实数值基线对照由 I40 完成。
- 新模式不存在静默 fallback；缺少对应能力显式报错。

## 必需证据

- 模式定义、数值调用/单位测试

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
