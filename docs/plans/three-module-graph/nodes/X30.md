# X30｜文档其余物性与模型能力逐项落地

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：document_capability。
依赖：G10, M00。
分支：`codex/tm1/x30`。
状态记录：`state/X30.json`。

## 任务

1. 追踪常物性、表格、拟合、CoolProp/REFPROP 和用户模型；复用已实现项，仅给缺口安排子节点。
2. REFPROP 许可/运行库缺失只阻塞对应适配器，不要求共享核心依赖商业库。
3. 张量表达、各向异性求解、LTE 等按原文逐步要求分阶段，不通过新增字段宣称已有算法。
4. 梯度/伴随仍只预留；需要新物理决策时创建明确子节点，不擅自选择。

5. 核对并按独立原子子节点迁移剩余内部非 SI 量；旧内核单位适配的过渡状态不冒充原文要求的全部内部 SI 已完成。

## 独占写入范围

- `sjtu_tpmshx/models/extensions/**`
- `sjtu_tpmshx/tests/model_extensions/**`
- `docs/plans/three-module-graph/decisions/property_extensions.md`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 原文明示的常物性、表格、拟合、CoolProp/REFPROP 与用户模型逐项有真实实现和对应测试；缺运行库或许可的必需项保持 blocked，不能仅因已记录缺口而置 done。
- 仅原文明确“逐步支持／可扩展／必要时／仅预留”的项目允许按原限定记录分期；不能把明确要求自行降为后续扩展。
- 若某个提供器受阻，协调者将 X30 细分为互不依赖的实际提供器节点及一个收口节点；已能工作的提供器继续，X30 收口等待全部必需子项。

## 必需证据

- 文档能力追踪、逐项测试与限制

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
