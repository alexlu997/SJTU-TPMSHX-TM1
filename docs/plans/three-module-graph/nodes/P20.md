# P20｜2D 独立前处理实现

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/p20`。
状态记录：`state/P20.json`。

## 任务

1. 在新职责目录实现物理网格/端口、几何/设计场、固定参数准备；按冻结的模型接口接收提供器。
2. 输出 SI、实际网格、只读设计与边界，不输出 _run_simple 等闭包，也不执行首次 SIMPLE。
3. 保持旧对齐、加密、分区平滑/投影适用范围和坐标规则，不顺便改成新的离散方法或真实任意三维设计场。
4. 开发期使用测试提供器与契约样例；不等待真实后端。旧流水线文件只读，接线留给 I20。

## 独占写入范围

- `sjtu_tpmshx/preprocess/two_d/**`
- `sjtu_tpmshx/tests/preprocess/test_2d_tm1.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 起始阅读位置

- `sjtu_tpmshx/pipelines/stages_2d.py`
- `sjtu_tpmshx/pipelines/solve_2d.py`

## 验收

- 人工与已有输入到 CaseData 的转换可独立测试；求解器入口被禁止时仍能准备算例。
- 结果中没有活动对象，实际网格和边界可检查；真实模型/数值一致性由 I20 验收。

## 必需证据

- 单元测试、网格/端口对照、序列化前对象检查

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
