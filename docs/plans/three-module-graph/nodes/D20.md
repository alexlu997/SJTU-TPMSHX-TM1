# D20｜FieldResult HDF5 与指标 JSON 读写

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10, E00。
分支：`codex/tm1/d20`。
状态记录：`state/D20.json`。

## 任务

1. 实现 results.h5 与 metrics.json，归档网格、通量、原始状态、警告、模型/代码版本及指标规格。
2. 区分正常完成、取消、失败、未收敛；诊断包不能以完整有效结果包的状态发布。
3. 拒绝不支持版本/必需字段缺失；处理相对资源，避免依赖旧会话缓存。

## 独占写入范围

- `sjtu_tpmshx/io/result_io.py`
- `sjtu_tpmshx/io/metrics_io.py`
- `sjtu_tpmshx/tests/io_tm1/test_result_io.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 在未导入求解后端的进程中往返读写人工结果与指标。
- 空值/非有限诊断按声明存储，不把 NaN 写成可报告指标或 JSON 中未定义的有效数。

## 必需证据

- 文件样例、往返/错误测试

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
