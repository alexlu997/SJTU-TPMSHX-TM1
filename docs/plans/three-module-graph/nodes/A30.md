# A30｜参数/拓扑优化的公共评估适配

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：architecture。
依赖：G10。
分支：`codex/tm1/a30`。
状态记录：`state/A30.json`。

## 任务

1. 优化器只负责设计向量、预算、目标正负号、归一化与惩罚；调用公开模块入口，不修改私有数组。
2. 拓扑优化只改变设计场/均质化参数场，通过现有 Solver 接口完成求解；第一阶段仅预留梯度/伴随接口，不急于实现。
3. 先用契约结果验证应用逻辑。旧 core/evaluators 和 evaluator*.py 的接线由 I53 完成。

## 独占写入范围

- `sjtu_tpmshx/optimization/public_evaluation.py`
- `examples/three_module/parameter_scan.py`
- `examples/three_module/design_field_call.py`
- `sjtu_tpmshx/tests/apps_tm1/test_optimization_adapter.py`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- 三类输入：参数修改、设计场修改、失败结果都可通过公开数据处理。
- 测试替身不算真实优化求解；伴随能力必须声明未实现。

## 必需证据

- 目标/单位/惩罚对照测试、公开调用示例

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
