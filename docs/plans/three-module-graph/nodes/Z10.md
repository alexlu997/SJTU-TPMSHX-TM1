# Z10｜文档能力里程碑 M-B 核对

状态：planned。该文件是任务卡，不是完成证据。

里程碑范围：document_capability。
依赖：Z00, H30, X11, X22, X30。
分支：`codex/tm1/z10`。
状态记录：`state/Z10.json`。

## 任务

1. 沿 V0.1 逐条对照已实现能力与分期限定；明确 required now、逐步、可选、仅预留。
2. 只有真实交付项可以完成；若某扩展仍受阻，报告 M-A 已完成与 M-B 未完成，而不是改需求清单。

## 独占写入范围

- `docs/plans/three-module-graph/acceptance_document.md`

这是逻辑归属，不要求一次创建全部路径；不存在的旧路径须先核查。可写自己的状态、证据与局部测试，不得修改其他节点所有文件。需变更范围时提交协调请求，禁止先改后补。旧大文件与公共契约不得由叶节点私自修改。

## 验收

- M-A 已完成；V0.1 所有本期明确必需要求均有实际实现与验收证据，缺失项不得通过改名为可选而关闭。
- 原文确有分期限定的项目如实保留分期状态；M-B 结论逐项列明已实现范围、未实施的后续能力，不宣布无条件全功能支持。
- C++/OpenFOAM 完成范围以各自真实模型、网格、流体和边界能力表为准；不将最小切片完成扩大为全部 Python 能力等价。

## 必需证据

- 最终需求追踪矩阵、后端与物性验收

证据必须写明代码提交、输入/数据版本、环境、命令、通过/失败/跳过数量、测试层级及未运行原因。测试替身通过只说明契约层通过；真实集成另有节点。

## 阻塞处理与交接

记录阻塞类型、已完成产物、下一步解除条件与受影响的后继；保存自己的分支/PR，不修改无关节点状态。调度者应立即转向其他 ready 节点；不可自动跳过必需验收。资源等待时释放计算配额，不长期持有共享写锁。

## 提交与合并

用户已授权本 Goal 范围内自主提交、推送、PR、CI 和达标合并。遵守 `OPERATIONS.md`；实现者不能用自己的总结替代独立审查和真实 CI。合并由单一协调者串行执行，合并后验证完成才置 done。
