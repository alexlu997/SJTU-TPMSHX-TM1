# 主计算结果一致性、精度与效率

状态：本地实施与验收完成；CI、合并和发布提交以对应 PR 的记录为准。
基准：`51eb05388b6a8833fada57242529c25222a08ea7`。
分支：`codex/tm1/accuracy-performance-20260914`。

用户已明确要求本轮继续完成压降精度研究后再交付，不接受先发布热计算
修复、将两侧压降目标移到下一阶段。Q≤0.5%、dP≤1%、Tout≤0.1 K
仍是新增网格目标；代表工况误差预算已达标，隔离性能检查已完成。

目标是在明确的数值精度和守恒条件下缩短完整求解时间。用户已同意上海
二维优先采用 106×24、单独优化 sCO₂ 三维压力求解、核查上海三维网格。
局部端口、入口总流量、生产 Nu/D-F 关联式和实验修正保持原定义；
上海二维水口按用户本任务确认改为均匀入口，其余物理配置保持原定义。
参数优化不在范围内。原始数据及完整算例证据留在本地 `data/`。

## 代码分析

- `postprocess/metrics.py`：二维 `Q` 在水/空气模式取两侧 Richardson
  外推绝对值的最大值；true_h 则取 A 侧原始边界焓流。
- `postprocess/three_d.py`：model_h 的 `Q` 取 A 侧原始焓流，true_h
  却通过报告流量、温度和压力重新构成出口焓；`thermal_duties` 已有原始证据。
- 三维 `result_capture.py` 的报告权重来自最终 SIMPLE 流动的绝对速度，
  而热场和守恒证据来自末次热计算。二维出口温度已使用末次热计算的向外
  质量通量。这既有状态差异，也有回流处理差异。
- `MetricSpec` 已含定义版本及说明；`PerformanceResult`/JSON 已可保存
  命名指标。优先复用，不另建结果协议或通用来源框架。
- `controllers/module_adapter.py` 已是 GUI 主指标的唯一映射入口，但
  部分残差仍从旧后端诊断复制，需要与公开指标对应。
- 上海验证入口采用 alpha=0.2 的历史启发式主网格；GUI 自动建议使用
  alpha=0.4，上海内置预设另外固定了 20×20×20。推荐策略和用户手填
  状态应分清；不能把一套网格强加给所有几何和流体。
- `simple_solver_3d.py::_solve_pp_amg` 已有 CSR 装配、矩阵规范化、
  AMG 层级复用及动态重建、适应性 BiCGStab 容差和直接法回退。
  现有重建/迭代计时可复用；应测实际热点，不重复引入缓存架构。

## 节点与完成条件

| 节点 | 实施 | 完成条件 |
|---|---|---|
| N0 基准 | 环境锁/pip check；保存既有工况清单；从原生存档量化两种 Q 和报告状态差异 | 基准来源、198 项分母、旧结果及失败记录可追溯 |
| N1 结果定义 | 完整计算统一 Q 为 A 侧主网格原生对流焓流绝对值；两侧有符号 Q 独立保存；Richardson 独立命名；温度/流量采用对应热场的有符号质量通量 | 手算回流/不平衡样例、旧原生存档重算、GUI 和 JSON/HDF5 交接通过；新定义标版本，旧冻结数值不覆盖 |
| N2 网格精度 | 上海二维已有 52×12/106×24/265×61 证据按新主指标重算；三维先对低/中/高工况做连续三层网格，并检查方向敏感性 | Q、两侧 dP、两侧 T_out 的变化及误差估计可解释；局部场使用相同物理位置；不把相邻差值直接称为真实误差 |
| N3 推荐网格 | 将通过精度要求的上海网格接入预设和生产验证入口；保留用户手填；首选二维 106×24，保留 Richardson 计算 | 上海全部 16 项各维度通过原检查；实际准备网格、边界和总流量正确；二维热缓存完整流程目标 2–4 s |
| N4 压力求解 | 对 sCO₂ 三维测装配/规范化、层级建立、线性迭代及重复调用；逐个试验最小改进 | 同物理、网格、容差下完整求解有可重复收益，原场与残差一致性合格；失败候选撤销并记录 |
| N5 交付 | 固定 166 项 sCO₂ 与上海 32 项验证；针对性/集成测试；四类冷热运行计时；文档、PR/CI 及获授权合并同步 | 分别报告软件、物理适用、守恒、数值精度、实验偏差、求解/交互耗时，未达项目明确保留 |

## 新结果定义的边界

`Q` 表示 A 侧在本次主网格热计算状态的对流焓流绝对值；A 是输入中
明确命名的一侧，不自动按温度更换。`Q_A`、`Q_B` 保留符号（热流体放热为正）。
原始能量不平衡仍独立计算，不能通过取最大值、平均或强制两侧相等消除。
二维为 W/m，三维为 W；整机比较只在应用边界乘一次实际厚度。

外推量与主指标独立命名，保持细网格收敛/适用域检查。缺少一侧证据时
该侧指标保持不可用，不能借另一侧伪造。压力仍是明确标注状态的最终
SIMPLE 端口压差，不宣称与末次热场完全相同；先量化其交接差异。
筛选和快速设计有独立近似模型，本次不重定义其指标。

## 精度及测量方法

用户已确认新增网格目标：Q 误差估计 ≤0.5%、两侧 dP ≤1%、
出口温度 ≤0.1 K（2026-09-14 本任务直接回复）。默认网格按这组目标验收。
上述新增目标不更改任何原有收敛、守恒、冻结数值或实验验收阈值。

三维第一组相似网格为 20×10×3、40×20×6、80×40×12；先覆盖上海
工况 1、8、16。保持原始矩形端口的实际覆盖面积和入口总流量。如果
解未收敛或未呈现可解释的加密趋势，先诊断，不能强套 Richardson/GCI。
必要的方向对照以同一平面网格改变 Nz，分清厚度分辨率与平面分辨率。
合格的候选再覆盖全部 16 项。二维现有网格不严格等比，按实际间距处理。
极小的换热量外推修正不能替代压降或局部场精度证据。

精度研究是有意改变离散网格；压力提速试验则固定网格和原阈值。
两者分开对照。压力候选先测代表工况，入选后做固定集回归；采用与此前
一致的完整求解中位数 ≥5% 收益标准，不把 G7 旧单项例外扩展到本次。
首次编译、热缓存求解、后处理/文件交接与 GUI 响应分别计时。
前后配对运行，保持固定线程和环境，排除预热样本，不叠加并发阶段时间。

参考方法：[NASA 网格收敛指南](https://www.grc.nasa.gov/www/wind/valid/tutorial/spatconv.html)。
数值收敛与实验准确性分别报告。没有压力测点证据的项目不声称通过实验
精度验证；不因此阻塞已授权的数值、代码和效率改进，也不拟合新系数。

## 进度

### 当前状态（后续实测更新）

上海推荐网格候选现为实际计数 84×24（二维）、92×14×10（三维），
按端口和壁面分配网格；106×24 均匀候选不再直接作为精度结论。
用户要求本轮完成压降精度研究后再交付，原 Q/压降/温度目标不变。
界面和验证入口已接线；三代表、两维度的30个误差预算项全部达标，
具体方法和适用边界见验证记录。最终隔离性能及本地发布检查已完成。

- 当前四类热求解中位数：sCO₂二维3.600s、三维4.212s；上海二维3.141s、
  三维14.145s。上海二维含后处理及文件回读的完整流程3.450s，达到2–4s目标。
- 修正端口后的48次压力配对全部运行合格，四代表求解提速76.59%–89.24%。
  最终场与主指标均在原比较内；用户另批准本次压力配对的诊断Q_net绝对1e-8W，
  连同此前中间温差/h_v例外，24/24对通过。原18/24的失败记录保留。
- 上海同网格24次能量加速对照全部合格：二维求解减少46.22%，三维减少10.05%。
  迭代轨迹并非1e-10逐场等价，采用依据为本轮完整数值误差预算及原收敛/守恒检查。

- 空间平均：非均匀网格改用物理面积/体积平均。用户批准建立新版软件回归参考，
  原参考及失败记录保留，128 项集成/界面/接口测试通过、1 项跳过。
- 端口边缘：消除浮点舍入产生的假开口，不按开口比例删减真实小端口。
  79 项端口和真实回归检查通过。
- 动量迭代：严格检查暴露 SOU 修正借用相邻面流向，新增共享面抵消测试在
  修复前四项失败。改为每个面选择自身迎风分支后，29 项检查通过，
  上海二维三个代表的 mom=1e-7、outer=0.001 K 检查全部合格。
  该修复影响的历史数值已单独对照，用户另外批准了下述离散修复参考版本。
- 完整固定集：端口/SOU 修复后 166/166 软件与流量通过；与旧数值仍有差异。
  用户已单独批准为这两项离散修复建立新数值参考版本，保留全部旧参考和失败；
  新版独立复算166/166通过，全部在原1e-10下重复通过。原比较容差及收敛、守恒条件未改。
- 上海完整集：端口修复后、SOU 修复前 32/32 合格；新 SOU 的二维 16/16 合格。
  76×24 的高负荷水压误差预算约0.996%，余量太小；84×24 同项降为约0.854%，
  84×24全部16项和三个严格迭代对照均合格。计入迭代差异后的最终水压预算0.866%。
- 二维参考：重新计算当前离散格式的三个代表、三层嵌套网格，并提高研究用迭代预算；
  生产预算和全部停止判据不变。三维已分别量化贴壁、流向和热迭代的影响，
  最终两侧压降最大预算为二维0.866%、三维0.896%。两个低负荷出口温度
  补第四个网格评估拟合散布后也满足0.1K目标，没有将相邻差值直接认作误差。

精确软件参考与旧值见[验证记录](../accuracy-performance-20260914.md)。
以下保留早期步骤的结果；它们不代替最新代码的最终验收。


- N0：环境匹配 71 个基础锁定包，pip check 通过；198 个原生存档对照完成。
  Q 定义变化：上海二维 −0.1543%..−0.0054%，上海三维与 sCO₂ 二维不变，
  sCO₂ 三维 +0.0097%..+0.2669%。固定集没有出口回流；三维原始温度
  与新温度指标仅有舍入差。sCO₂ 热场与报告压力状态最大差约 72.45 kPa，
  新 Q 不再重算另一压力状态下的焓。上海温度形式存档未记录 P_thermal，
  该压力状态对照明确不可用，不据此推断相等。
- N1：主指标及回流权重修改完成；后处理/文件测试 21 项和真实计算、
  GUI/导出/交接测试 16 项通过。安装示例 Q 按新定义更新；后端冻结值保留。
- N2：上海二维 16 项既有三层网格已按新 Q 重算。106×24 对 265×61 的
  最大差值为 Q 0.00442%、气侧压降 0.744%、水侧压降 1.185%、出口温度
  0.00298 K。这些是网格间差值，尚不能认证新增精度目标。
  二维所有这些网格原本已通过 `_aligned_grid` 对齐端口；补做的
  52×12/104×24/208×48 是严格倍增对照，不是新增端口对齐功能。
  水入口边缘现有四格降速区会随网格改变物理宽度，需要先确定固定入口
  分布，才能进行同一边界下的纯网格误差研究。用户现已确认上海水侧使用
  均匀入口；配置、预处理、SIMPLE 和 GUI/预设已接线，相关 65 项测试通过。
  新入口下的网格研究单独记录，不与上述旧入口结果混作三层误差估计。
- N2：上海三维工况 1/8/16 的三层网格、厚度对照、加细及停止容差对照，
  连同现有贴壁加密探针共 20 次计算均完成并通过软件/入口流量检查。
  600 单元网格的水侧压降与 38,400 单元网格相差约 4.6%–5.0%。
  厚度加密的压降差值未持续收缩；工况 1 的贴壁网格与 129,600 单元
  普通网格的两侧压降仍相差约 1.8%–1.9%。单个贴壁探针也不是已收敛
  参考解。因此当前证据不足以认证三维压降误差 ≤1%，不更新默认网格。
- N3：推荐接线等待 N2 的固定入口条件及精度证据；106×24 仍是候选，
  未标为通过精度认证。
- N4：压力固定行的单位对角元掩盖了活动矩阵漂移；活动系数相对变化
  超过 99% 时，旧全局对角范数只变化不到 0.12%。修复改为非固定行
  对角向量的相对变化，保留原 5% 重建门槛、求解容差及回退。
  四个 sCO₂ 三维代表各版本预热一次、交错测五次，共 48 次均完成并
  通过软件/入口流量检查。完整求解中位数 D8 19.60→4.57 s、
  D19 30.66→4.30 s、G7 29.26→3.09 s、G41 15.44→2.96 s。
  最终主指标及 T/P/速度场满足原比较限值；中间 outer_dT 最大差
  2.864×10⁻⁹ K，G7 的 h_vB 最大相对差 1.723×10⁻¹⁰，仍有原比较
  限值失败。用户已明确接受中间 outer_dT 绝对 10⁻⁷ K、派生 h_v 场相对
  10⁻⁸；其余最终场/指标仍按原 10⁻¹⁰ 比较，求解停止和守恒不变。
  24 组配对按批准标准全部通过，原比较失败记录保留。
- N5：全库快速集 3,073 项通过、5 项跳过，另 5 项旧 Q/能量闭合断言
  失败；已按新公开定义修复，后端冻结数值仍单独保留。相关复测、全部
  三模块集成及压力重建测试共 83 项通过、1 项跳过，包含上述 5 项。
  跳过项是需要 CI 提供独立交接目录的最小安装检查。
  固定 166 项 sCO₂ 已全部完成并通过软件、入口流量和批准后的数值比较。
  原限值下 83 项存在超限，批准标准下为 0 项；两种检查结果均保留。
  此批基于提交 `8bb91b3`，上海均匀入口后的相关回归另行记录。
  均匀入口提交 `b76b484` 后，83 个固定 sCO₂ 二维和 16 个上海三维
  在原网格上复算，99/99 软件、流量和原数值比较均通过，原比较失败为 0。
  PR/CI/合并及本地同步尚未完成。
  完整证据写入本地 `.cache/accuracy-performance-20260914/`。

## 新发现的定义差异

- 压降：二维现行报告使用首末单元中心压力及平滑端口权重；三维使用
  外推边界面压力和几何面积权重。只读对照已量化差异，未更改生产压降
  或用于 sCO₂ 压力闭合的锚点；后续统一必须区分报告定义与物理压力锚。
- Nu 的局部输入：二维用速度模长，三维用配置主方向的速度分量；
  水/空气的物性更新也有场均值与入口值的差别。上海加密后两维度 Q
  仍相差约 4.6%，目前不能把该差异归因于网格或某一个模型因素。
  本阶段不擅自改动已冻结关联式及物性基准，也不宣称两维度物理等价。

## 压降研究接续

- 二维压力报告正在改为真实端口面外推及几何面积权重，定义版本
  `pressure_face_v1`。11 项后处理测试通过；旧后端压力参考和物性压力锚
  保留。报告公式的统一本身不能证明压降达到 1%。
- 端口与贴壁网格试验发现，原出口压力固定在最后一排单元中心；
  20 μm 的激进加密会出现出口角落回流。实际出口面压力加半控制体
  法向动量的隔离候选，在二维、三维离散充分发展 Brinkman 解上分别
  保持约 10⁻¹⁵ Pa 的压力误差。新增出口方程进入原 F2 动量残差。
  候选尚未并入生产；与原出口离散的对照也保留，避免无收益的扩展。
- 固定均匀水入口下，92×20、184×40、368×80 局部加密网格的
  3 个二维代表工况共 9 次均通过软件、入口流量及热场检查。
  细网格交接曾产生约 7×10⁻¹⁶ kg/(s m) 的墙面流入，根因为端口
  坐标累加舍入；试验网格改为逐段锚定端点、细分保持父单元端点后
  消失，未放宽回流检查。参考网格允许最多 15,000 次 Richardson
  热迭代，原停止阈值不变；生产的 5,000 次上限未改。
  此组三层水侧压降粗网格 GCI 约 0.25%–0.58%，但高负荷气侧仍
  未进入稳定收敛区，不能据此认证整体目标。继续研究气侧薄贴壁层。
- 非均匀网格还暴露了用于物性计算的算术平均：例如低负荷二维局部
  网格的气侧温度算术平均约 310.65 K，面积平均约 303.93 K。
  正在将物性及压力种子所用的空间平均改为实际面积/体积加权。
  二维、三维单元细分不改变物理平均值的 2 项检查通过；此前网格
  结果均保留，新的平均权重下需要重新验证，不能混为同一组三层证据。
- 三维 92×20×12 候选网格的 SIMPLE 通过，但默认热松弛 0.7
  在四次热求解中均耗尽 20,000 次，完整试算约 263 s 且不合格。
  直接套用现有 Anderson 混合仍不收敛，约 269 s，不采纳该候选。
  在现有松弛 0.4 下同一网格完整求解约 44.6 s 且合格；再加混合
  初测约 37.5 s 且合格。它们是隔离单次试验，不是正式性能中位数，
  更细网格和最终采用方案仍待验证。

### 局部加密与迭代误差分离（继续执行）

- 20 μm 气侧贴壁层的二维三网格研究已经完成：92×28、184×56、
  368×112，低/中/高三工况共9次均合格。中、高负荷气侧粗GCI
  分别0.0242%、0.0252%；三工况水侧粗GCI为0.317%、0.545%、0.642%。
  低负荷气侧需分离迭代误差：固定同一细层温度场后的压力三网格
  得到1.83阶、粗GCI0.0357%。该压力诊断不能独立代替完整耦合验收。
- 二维压力种子中的黏度平均也改为面积权重，保持与已修正的物性
  空间平均一致。改变该权重前后的证据分别保留。
- 原出口算法在上述粗网格的三个工况均合格，结果与半控制体出口
  候选接近，因此优先保留原出口实现。66×24的压降仍不满足目标；
  76×24局部加密候选的三次完整求解均合格，探索耗时2.65–3.49 s，
  相对已有细参考 Q差≤0.044%、出口温度差≤0.028 K、水压差≤0.846%。
  原细层GCI计入后水压估计约0.91%；最终同源参考、迭代误差和全部
  上海工况尚待复验，网格及热迭代混合仍未并入生产。
- 三维松弛A=.7/S=1/B=.4在中负荷细化后仍振荡；同一已保存的
  LTNE输入改用现有水侧松弛B=.1，2000次约5.45 s收敛，原阈值不变。
  三个工况、三层完整计算继续运行，低负荷三个网格已全部合格；
  中层气侧压降GCI0.744%，水侧的微小变化尚无稳定观测阶。
  三维局部贴壁候选和压力误差分离继续进行。探索并行运行的耗时
  不作为正式性能中位数；最终性能验收仍独立运行。
