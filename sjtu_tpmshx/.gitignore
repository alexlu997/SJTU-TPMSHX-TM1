# demo_output: regenerate via runs/demos/demo_3d_*.py.
# production_output: LEGACY optimizer sweep outputs — the generator scripts
# were retired; contents are local-only history, not regenerable (the old
# comment named three scripts that no longer exist).
runs/demo_output/
runs/production_output/

# Test-run artefact: regenerated every `pytest tests/test_shanghai_regression.py`
validation/shanghai_3d_baseline_pytest_h3.csv

# 3D solver profiler trigger (opt-in; see pipelines/run_stack_3d.py _prof_3d_enabled)
.profile_3d
